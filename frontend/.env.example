# Copy to .env.local to override. Where the backend API runs (no trailing slash).
# Use the same hostname you open the frontend on (localhost vs 127.0.0.1), or the browser will not
# send the login cookie and every reload logs you out.
VITE_API_URL=http://localhost:3000
